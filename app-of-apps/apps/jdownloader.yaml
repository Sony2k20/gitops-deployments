apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jdownloader
  namespace: argocd
  labels:
    type: app
spec:
  destination:
    namespace: prod
    name: in-cluster
  project: default
  source:
    repoURL: https://sony2k20.github.io/service-deployer-helm
    targetRevision: 1.0.9
    chart: service-deployer
    helm:
      values: |
        fullnameOverride: jdownloader

        dailyRestart: true

        image:
          repository: jlesage/jdownloader-2
          tag: v25.07.2

        service:
          port: 5800
          
        ingress:
          host: jdownloader.csnguyen.de

        startupProbe:
          httpGet:
            path: /
            port: 5800

        persistentVolume:
          - name: jdownloader-config
            mountPath: /config
          - name: jdownloader-downloads
            mountPath: /output
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
