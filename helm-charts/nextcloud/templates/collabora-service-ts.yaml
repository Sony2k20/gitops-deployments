apiVersion: v1
kind: Service
metadata:
  name: collabora-ts
  namespace: prod
spec:
  type: NodePort
  ports:
    - port: 9980
      targetPort: 9980
      nodePort: 30013
  selector:
    app.kubernetes.io/component: app
    app.kubernetes.io/instance: nextcloud-collabora  
    app.kubernetes.io/name: nextcloud-collabora  